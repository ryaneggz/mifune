# Skills

This directory is intentionally empty in the standalone Mifune pack.

In Open Harness, `.pi/skills` was a symlink to `../.claude/skills`. That
symlink is not portable, so the extracted repo ships a real directory instead
of a dangling link. Add project-specific Pi skills here when installing Mifune
into a workspace.
